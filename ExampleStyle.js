import React from 'react';
import {View, StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: 'red',
  },
  defaultText: {
    fontSize: 70,
  },
});
